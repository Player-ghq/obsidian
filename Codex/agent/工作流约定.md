# 工作流约定

## Obsidian 持久记忆

记忆目录：`/Users/HaoQi/Documents/Codex/obsidian-vault-codex-codex-codex-todo-2/Codex`

重要工作会话开始前：

1. 快速浏览 `Codex/AGENTS.md`。
2. 按当前任务读取相关记忆文件。
3. 不遍历整个记忆库。

工作中如出现值得长期保留的人员背景、项目背景、关键决策、待办事项、重复性工作流、项目状态或可复用想法，应更新对应 Markdown 文件。

重要工作会话结束前：

1. 判断本次是否有值得长期记录的内容。
2. 如有必要，更新对应的 Codex 记忆文件。
3. 告诉用户修改了哪些记忆文件；若未修改，也明确说明。
4. 将未解决事项记录到 `Codex/TODO.md` 或 `Codex/agent/open-loops.md`。

## 记忆边界

- 不直接转储完整聊天记录。
- 不保存密码、密钥、凭证、令牌或隐私信息。
- 只保存跨对话仍有复用价值的信息。
- 更新应简洁、具体、可检查。
- 优先维护已有文件。

## 知识库存储决策

- Obsidian 是当前唯一的知识沉淀位置。
- 资料、复盘、洞察、项目记录和长期记忆均写入本 Vault。
- 以后和 Codex 对话中新创建的项目，默认直接放入 `Codex/projects/`；仅临时草稿、脚本、实验文件放在当前 workspace 的 `work/` 或 `outputs/`。
- 思源笔记暂时停用，不再新建、同步或更新思源内容。
- 不自动迁移、覆盖或删除思源中的旧内容；如以后恢复使用，需由用户重新确认迁移范围和规则。

## 本地语料库

- 2026-08-01：已新建隔离知识库 `Codex/projects/肌动学与功能解剖学语料库/`，包含《基础肌动学 第3版》和《骨骼肌肉功能解剖学 第2版》的本地抽取文本、清洗层、章节汇编、结构化章节、概念卡、主题索引、问答路由、部位速查、考研答题模板和检索映射。以后回答这两本书相关问题时，优先按 `index/answer-routing-map.md` -> `index/body-region-quick-map.md` -> `index/exam-answer-templates.md` -> `structured/` -> `concepts/` -> `structured/compiled-chapters/` -> `cleaned/` -> `chunks/` -> `extracted/` 的顺序检索，不重新读取 PDF；回答中尽量标明书名与页码范围。
- 2026-08-02：该知识库已补入并系统整理北体《运动解剖学》第4-9章运动系统，范围为书页 p.30-238、PDF 页 43-251，来源为 OCR 清洗语料，统一标记 `OCR待复核`。已建立 6 个结构化章节、20 张概念卡、专项答题模板和路由入口。以后回答北体《运动解剖学》运动系统问题时，优先查 `index/beiti-motion-system-structured-index.md`、`structured/运动解剖学-北体/` 和 `concepts/北体运动系统/`，必要时再回到 `structured/compiled-chapters/运动解剖学-北体/`、`cleaned/by-book/运动解剖学-北体/` 和 `extracted/运动解剖学-北体/`。

## 已安装本地 Skills

- 2026-07-15：已在 `~/.codex/skills/` 安装 `zhihu-search` 与 `zhihu-hotlist` 两个本地 skill，来源为知乎开发者文档 `https://developer.zhihu.com/docs?key=authorization`。二者默认读取环境变量 `ZHIHU_ACCESS_SECRET`，也支持各自目录内权限为 `600` 的本地 `config.json`；不要把 Access Secret 写入记忆库或对话总结。
- 2026-07-15：已在 `~/.codex/skills/` 安装 `anki-excel-card-maker`，用于将学习资料生成 Anki 制卡 Excel 母版与 UTF-8 TSV 导入文件；规则参考 Anki 官方文本导入文档 `https://docs.ankiweb.net/importing/text-files.html`。凡涉及 Anki 制卡、闪卡、记忆卡、TSV/Excel 导入文件，优先读取并使用该 skill，而不是临时手写导出脚本。
- 2026-08-03：已在 `~/.codex/skills/` 安装 `anki-addon-developer`，用于创建、修改、调试、移植和打包 Anki 桌面插件；规则参考 Anki 官方插件开发文档 `https://addon-docs.ankiweb.net/intro.html`，reference 已按官方 20 个一级章节逐章覆盖。凡涉及 `addons21`、`__init__.py`、`aqt/anki` API、`gui_hooks`、webview、config、`user_files`、`.ankiaddon` 打包或插件移植，优先读取并使用该 skill。
- 2026-08-01：外刊精读 Anki 制卡默认规则：所有卡片 `Tags` 字段统一使用 `外刊精读`；TSV 文件头或导入设置的牌组使用 `#deck:英语::外刊精读`。如用户另有明确指定，以用户当次指定为准。
- 2026-07-28：已在 `~/.codex/skills/` 安装 `english-magazine-close-reading`，用于用户提供英文杂志、报刊、社论、长文、考研阅读式文章或整期杂志 PDF 时，按单篇文章或整期 PDF 两种模式处理。单篇文章按 Extensive Reading 与 Intensive Reading 两阶段做全英文外刊精读；整期 PDF 先读取目录、整理文章、分类、按考研英语价值排序，再推荐精读顺序。默认服务考研英语能力提升，兼顾信息提取、长难句、词汇、翻译、篇章结构和写作迁移；文章筛选重点优先关注文化与文化交流、教育学习、经济与日常经济生活、科技生活、健康饮食、环境保护、道德文明与公共伦理、精神品质、职场生活与职业发展、社会责任等主题。

## Git 分支收尾偏好

- 2026-07-26：用户明确要求以后功能分支收尾默认选择 1，即本地合并回 `main`、重新验证、清理 worktree 和本地 feature 分支；除非用户另有明确要求，不再每次询问 1/2/3。
