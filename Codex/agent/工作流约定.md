# 工作流约定

## Obsidian 持久记忆

记忆目录：`/Users/HaoQi/Documents/Codex/obsidian-vault-codex-codex-codex-todo-2/Codex`

重要工作会话开始前：

1. 快速浏览 `Codex/AGENTS.md`。
2. 按当前任务读取相关记忆文件。
3. 不遍历整个记忆库。

工作中如出现值得长期保留的人员背景、项目背景、关键决策、待办事项、重复性工作流、项目状态或可复用想法，应更新对应 Markdown 文件。

重要工作会话结束前：

1. 判断本次是否有值得长期记录的内容。
2. 如有必要，更新对应的 Codex 记忆文件。
3. 告诉用户修改了哪些记忆文件；若未修改，也明确说明。
4. 将未解决事项记录到 `Codex/TODO.md` 或 `Codex/agent/open-loops.md`。

## 记忆边界

- 不直接转储完整聊天记录。
- 不保存密码、密钥、凭证、令牌或隐私信息。
- 只保存跨对话仍有复用价值的信息。
- 更新应简洁、具体、可检查。
- 优先维护已有文件。

## 知识库存储决策

- Obsidian 是当前唯一的知识沉淀位置。
- 资料、复盘、洞察、项目记录和长期记忆均写入本 Vault。
- 以后和 Codex 对话中新创建的项目，默认直接放入 `Codex/projects/`；仅临时草稿、脚本、实验文件放在当前 workspace 的 `work/` 或 `outputs/`。
- 思源笔记暂时停用，不再新建、同步或更新思源内容。
- 不自动迁移、覆盖或删除思源中的旧内容；如以后恢复使用，需由用户重新确认迁移范围和规则。

## 已安装本地 Skills

- 2026-07-15：已在 `~/.codex/skills/` 安装 `zhihu-search` 与 `zhihu-hotlist` 两个本地 skill，来源为知乎开发者文档 `https://developer.zhihu.com/docs?key=authorization`。二者默认读取环境变量 `ZHIHU_ACCESS_SECRET`，也支持各自目录内权限为 `600` 的本地 `config.json`；不要把 Access Secret 写入记忆库或对话总结。
- 2026-07-15：已在 `~/.codex/skills/` 安装 `anki-excel-card-maker`，用于将学习资料生成 Anki 制卡 Excel 母版与 UTF-8 TSV 导入文件；规则参考 Anki 官方文本导入文档 `https://docs.ankiweb.net/importing/text-files.html`。凡涉及 Anki 制卡、闪卡、记忆卡、TSV/Excel 导入文件，优先读取并使用该 skill，而不是临时手写导出脚本。

## Git 分支收尾偏好

- 2026-07-26：用户明确要求以后功能分支收尾默认选择 1，即本地合并回 `main`、重新验证、清理 worktree 和本地 feature 分支；除非用户另有明确要求，不再每次询问 1/2/3。
